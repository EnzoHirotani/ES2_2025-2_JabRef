name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java-version: [17, 21, 24]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java-version }}
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Generate journal-list.mv
        run: ./gradlew :jablib:generateJournalListMV --no-daemon

      - name: Build (assemble + tests)
        run: ./gradlew build --no-daemon --info
        env:
          JAVA_OPTS: "-Djava.awt.headless=true -Dheadless=true"

      - name: Upload build artifacts (optional)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: build-libs-${{ matrix.java-version }}
          path: build/libs/**

      - name: Upload test reports (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ matrix.java-version }}
          path: build/reports/tests/**

      - name: Publish JUnit test results
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: 'Gradle Tests (Java ${{ matrix.java-version }})'
          path: '**/build/test-results/test/TEST-*.xml'
          reporter: java-junit
